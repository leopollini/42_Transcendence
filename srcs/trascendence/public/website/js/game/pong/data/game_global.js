export let pongCustomData = {
  ballColor: "#ffffff",
  paddleColor: "#ffffff",
  ballTrailColor: "#ffffff",
  wallsColor: "#ffffff",
  background: "space",
  powerUpActive: false,
};

export let matchData = {
  player1: "",
  player2: "",
  seconds: 0,
  matchTime: "",
  scorep1: 0,
  scorep2: 0,
  longestRally: 0,
  timer: 0,
};

/*export function setBallColor(color) {
  pongCustomData.ballColor = color;
}

export function setPaddleColor(color) {
  pongCustomData.paddleColor = color;
}

export function setBallTrailColor(color) {
  pongCustomData.ballTrailColor = color;
}

export function setWallsColor(color) {
  pongCustomData.wallsColor = color;
}

export function setPowerUpState(state) {
  pongCustomData.powerUpActive = state;
}

export function setBackground(bg) {
  pongCustomData.background = bg;
}*/

// export function setPowerupPlayer(powerup, player) {
//   if (player === 1) {
//     powerupPlayer1 = powerup;
//   } else if (player === 2) {
//     powerupPlayer2 = powerup;
//   }
// }
